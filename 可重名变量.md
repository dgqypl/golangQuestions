以下程序的输出是什么？
```go
import "fmt"

var block = "package"

func main() {
	block := "function"
	{
		block := "inner"
		fmt.Printf("The block is %s.\n", block)
	}
	fmt.Printf("The block is %s.\n", block)
}
```

答案：
```bash
The block is inner.
The block is function.
```